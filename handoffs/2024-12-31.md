# The Pause - Session Handoff

> Last updated: December 31, 2024

## Current State

**App is runnable** - `npm run dev` starts Expo on port 8081. Voice coach works with Gemini + ElevenLabs.

### What Was Completed This Session

1. **TRUE Radial Layout** - PAUSE home screen now has exercises positioned in a circle around the orb (not a horizontal row)
   - 5 exercises: Breath (top), Light (top-right), Count (bottom-right), Talk (bottom-left), Relax (top-left)
   - Uses trigonometric positioning with CSS transforms
   - Dashed SVG circle ring around the orb
   - Golden dot indicator on selected exercise

2. **Canonical Gold Color** - Switched from `#EBB305` to `#d4a954` (Stitch design canonical)
   - Updated: `tailwind.config.js`, `constants/colors.ts`, all pause components
   - Updated ALL screens: tabs, onboarding, coach, library, settings, habits

3. **PAUSE Screen Header**
   - Streak counter (fire icon + count) top-left
   - Settings button top-right

4. **GoldenOrb Component**
   - Added `size` prop for flexible sizing
   - Breathing animation: scale 1.0 → 1.08

5. **Environment Variables Fixed**
   - Changed to `EXPO_PUBLIC_` prefix (required by Expo for client-side access)
   - Voice coach now connects properly

6. **Floating Pill Tab Bar**
   - Matches Stitch design with rounded pill shape
   - Floating above screen with shadow
   - No labels, icon-only with highlight backgrounds
   - Elevated center orb button

### Key Files Modified

| File | Changes |
|------|---------|
| `app/(tabs)/index.tsx` | Complete rewrite with radial layout, header, modals |
| `components/pause/RadialSelector.tsx` | TRUE radial positioning with SVG dashed circle |
| `components/pause/GoldenOrb.tsx` | Size prop, canonical gold colors |
| `components/pause/IntentionScreen.tsx` | Updated gold colors |
| `constants/exercises.ts` | 5 exercises with angle positions |
| `constants/colors.ts` | Canonical gold palette |
| `tailwind.config.js` | Stitch gold colors |
| `.env.local` | EXPO_PUBLIC_ prefixed keys |
| `services/gemini.ts` | EXPO_PUBLIC_GEMINI_API_KEY |
| `services/elevenlabs.ts` | EXPO_PUBLIC_ELEVENLABS_* |

---

## Technical Stack

- **Framework**: Expo SDK 53 + React Native 0.81
- **Styling**: NativeWind v4 (Tailwind CSS v3)
- **Navigation**: Expo Router (file-based)
- **Backend**: Convex + Clerk auth
- **Voice Coach**: Gemini 2.0 Flash + ElevenLabs TTS
- **Animations**: React Native Reanimated

### Environment Variables (`.env.local`)

```
EXPO_PUBLIC_GEMINI_API_KEY=...
EXPO_PUBLIC_ELEVENLABS_API_KEY=...
EXPO_PUBLIC_ELEVENLABS_VOICE_ID=gn39AkrRBGmOUvxXfY8S
```

---

## Design Reference

Stitch HTML designs saved in: `design/stitch-outputs/STITCH_DESIGNS_REFERENCE.md`

### Color Palette (Canonical)

| Token | Value | Usage |
|-------|-------|-------|
| Primary Gold | `#d4a954` | Buttons, accents, orb |
| Gold Light | `#e8c87e` | Highlights, orb center |
| Gold Dark | `#b8923f` | Pressed states |
| Text Gold | `#FFEBB8` | Coach messages |
| Background | `#0A0E1A` | Main background |
| Surface | `#162235` | Cards, inputs |

---

## What's Next (Prioritized)

### High Priority
1. **During Pause Screen** - Audio waveform visualization, timer, progress bar, captions
2. **Library Screen** - Match Stitch design with tab underlines, 64px icons, floating nav
3. **Coach Screen** - Dr. Miller avatar, gold message bubbles, horizontal quick prompts

### Medium Priority
4. **Onboarding Flow** - 5 screens exist but need Stitch polish
5. **Progress/Habits Screen** - Dot grid is "best looking" per user, minor tweaks
6. **Settings Screen** - Mostly complete

### Technical Debt
- iOS Simulator needs Xcode CLI tools configured (`sudo xcode-select -s /Applications/Xcode.app`)
- TypeScript errors in auth screens (unrelated to PAUSE work)
- Pre-existing elevenlabs.ts type errors (FileSystem API)

---

## Commands

```bash
# Development
npm run dev              # Start Expo (port 8081)
npx convex dev           # Start Convex backend

# Voice coach web preview
npx tsx scripts/coach-web-server.ts

# Build
npm run build:web
npx tsc --noEmit         # Type check
```

---

## Project Structure (Key Paths)

```
app/
├── (tabs)/
│   ├── index.tsx        # PAUSE home (radial layout) ✅
│   ├── coach.tsx        # Voice coach
│   ├── library.tsx      # Content library
│   ├── habits.tsx       # Progress dots
│   └── settings.tsx     # Settings
components/pause/
├── GoldenOrb.tsx        # Animated orb ✅
├── RadialSelector.tsx   # TRUE radial layout ✅
├── ActiveSession.tsx    # During-pause timer
├── IntentionScreen.tsx  # Post-pause capture ✅
└── ContextualBanner.tsx # Time-based suggestions
constants/
├── exercises.ts         # 5 exercises with angles ✅
└── colors.ts            # Canonical palette ✅
services/
├── gemini.ts            # LLM coach ✅
└── elevenlabs.ts        # TTS ✅
design/stitch-outputs/
└── STITCH_DESIGNS_REFERENCE.md  # Design spec
```

---

## User Decisions (Recorded)

- **Radial approach**: CSS transforms (not absolute positioning or SVG paths)
- **Gold color**: `#d4a954` (Stitch canonical)
- **Exercises**: All 5 free for MVP (Breath, Light, Count, Talk, Relax)
- **Priority screen**: PAUSE home first (completed)

---

## Known Issues

1. **xcrun simctl error** - Xcode CLI tools pointing to CommandLineTools, needs `sudo xcode-select -s /Applications/Xcode.app`
2. **Package version warnings** - react-native and react-native-worklets slightly out of sync (non-blocking)

---

*Use `/handoff` to update this document at session end.*
